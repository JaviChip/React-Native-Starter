import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import AppNavigation from "../React-Native-Starter/navigation/AppNavigation"

export default function App() {
  return (
    <AppNavigation/>
  );
}
